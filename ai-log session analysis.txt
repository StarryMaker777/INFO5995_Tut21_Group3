
Q1: I have decompiled an Android APK using JADX and imported the source code into VS Code.

The application code appears to be located under:
com.example.mastg_test0016

There are only a few classes in this package:
login.java;MainActivity.java;Profile.java;R.java
Before diving into detailed code analysis, I want to identify the best strategy to locate authentication-related logic.

What would be a good approach to:
1. Identify where login or authentication happens
2. Trace how authentication state or user data flows across these classes
3. Avoid missing cross-file logic when analyzing such a small codebase



Worked for 25s
A1: A good way to approach this APK is to start from entry points and data stores, then draw a tiny auth-flow map before reading line by line.

In this codebase, the likely authentication path is already quite visible:

App entry starts in AndroidManifest.xml (line 35), where MainActivity is the launcher.
MainActivity collects username/password from UI and writes them to credentials.txt in MainActivity.java (line 31) and MainActivity.java (line 54).
Login reads credentials.txt and compares input credentials in Login.java (line 49) and Login.java (line 71).
On success, Login creates a session token in shared prefs and navigates to Profile in Login.java (line 58) and Login.java (line 174).
Profile only clears that session token; it does not appear to verify it before showing the screen in Profile.java (line 32) and Profile.java (line 49).
For your three goals, I’d use this workflow:

Identify login/auth logic by searching for:
startActivity, Intent, SharedPreferences, openFileInput, openFileOutput, getString, putString, remove, and button click handlers.
In this app, that immediately reveals the auth-related primitives are credentials.txt and SessionPrefs.

Trace auth state by following storage, not class names:
MainActivity writes credentials -> Login.checkCredentials() reads them -> Login.createSession() writes sessionToken -> Profile.clearSession() removes it.
That gives you both the credential flow and the session flow.

Avoid missing cross-file logic by making a 1-page table:
MainActivity: credential creation/navigation
Login: credential verification/session creation
Profile: logout/session deletion
AndroidManifest.xml: true entry point and reachable activities
This is especially useful with JADX because decompiled code can look noisy, but the real control flow is often small.

Two practical tips for small decompiled apps:

Ignore R.java for logic; use it only to map UI IDs if needed.
Don’t trust class names alone. In this sample, “authentication state” is not enforced anywhere obvious, which is exactly the sort of cross-file issue a data-flow pass catches.
If you want, I can do the next step and produce a compact auth-flow diagram plus a list of likely security weaknesses in these three classes.


Q2: Please help me verify this authentication flow more carefully.

Based on the code:
1. Where exactly is credentials.txt created and what does it store?
2. How does Login validate the user input against that file?
3. Where is the session token created, and what is actually stored in SharedPreferences?
4. Does Profile verify the session token before showing the screen, or does it only clear it on logout?

Please distinguish clearly between:
- direct evidence from the code
- assumptions or possible security implications



A2: Worked for 2s
Direct Evidence

credentials.txt is created in MainActivity.java (line 54). The method saveCredentialsToFile(String str, String str2) calls openFileOutput("credentials.txt", 32768) and writes:
"Username: " + str + " Password: " + str2 + "\n"
See MainActivity.java (line 62) and MainActivity.java (line 63).

What it stores, based on the code, is plaintext username/password pairs in this format:
Username: <username> Password: <password>

Login validates user input in Login.java (line 71). The method checkCredentials(String str, String str2) does this:
Opens credentials.txt with openFileInput("credentials.txt") in Login.java (line 77)
Reads it line by line with BufferedReader in Login.java (line 82) and Login.java (line 85)
Splits each line on spaces in Login.java (line 87)
Expects exactly 4 parts: Username:, <user>, Password:, <pass> in Login.java (line 88)
Compares the entered username and password directly with the parsed file values in Login.java (line 93)
If a match is found, it returns true in Login.java (line 103). Otherwise it eventually returns false on error, and for non-matching lines it keeps reading.

The session token is created in Login.java (line 174). createSession() stores a generated token with:
this.editor.putString(KEY_SESSION_TOKEN, generateSessionToken());
this.editor.apply();
See Login.java (line 175) and Login.java (line 176).

The key name is sessionToken and the preferences file name is SessionPrefs, defined in Login.java (line 26) and Login.java (line 27). The token value is a 16-character random alphanumeric string generated in Login.java (line 183).

So what is actually stored in SharedPreferences is:

prefs file: SessionPrefs
key: sessionToken
value: a random 16-char alphanumeric token
Profile does not verify the session token before showing the screen, based on the code shown. In onCreate(), it immediately calls setContentView(...) and initializes SharedPreferences in Profile.java (line 22) through Profile.java (line 34). The only session-related action is the logout button calling clearSession() in Profile.java (line 35) and Profile.java (line 49).
clearSession() only removes sessionToken from SessionPrefs:

Profile.java (line 50)
Profile.java (line 51)
Assumptions / Security Implications

The code strongly suggests credentials are stored in plaintext in app-internal storage. That is not an assumption about format; the plaintext write is directly visible. The implication is that credentials are not hashed or encrypted before storage.
MainActivity appears to behave like a registration or credential-seeding screen, because it saves whatever username/password the user enters and then moves to Login. That interpretation is inferred from behavior, not explicitly labeled in code.
I do not see any check in Profile like “if token missing, redirect to Login.” So based on these files, Profile looks accessible once launched, regardless of session state. The implication is weak session enforcement.
The token appears to be only a presence marker. In the code shown, nothing binds it to a user identity, expiry time, or server-side validation. That is an inference from the absence of such logic in these classes.
Login has a getSessionToken() method in Login.java (line 179), but I do not see it used in the classes you asked about. That suggests the token may not actually gate access anywhere in this small codebase.
The verified flow is:
MainActivity writes credentials -> Login reads and compares them -> Login stores sessionToken in SessionPrefs -> Profile can clear that token, but does not appear to check it before display.

If you want, I can turn this into a concise auth-flow diagram or a small table of findings with evidence vs implication.


Q3: I manually verified the following in Profile.java:

- KEY_SESSION_TOKEN is defined
- SharedPreferences is initialized
- there is no getString(...) call
- there is no getBoolean(...) call
- clearSession() removes KEY_SESSION_TOKEN
- I do not see an explicit conditional check on sessionToken before the activity is displayed

Based on this evidence, is it reasonable to conclude that Profile does not enforce session validation?

Please separate:
1. direct code evidence
2. security implication
3. what would still need dynamic testing to confirm



A3: Yes. Based on this code alone, it is reasonable to conclude that Profile does not enforce session validation.

1. Direct Code Evidence

In Profile.java, onCreate() immediately calls setContentView(...) before any session-related read or guard logic:

Profile.java (line 21)
Profile.java (line 25)
The activity initializes SharedPreferences and an editor:

Profile.java (line 32)
Profile.java (line 34)
But there is no visible read of the stored session value in this class:

no getString(KEY_SESSION_TOKEN, ...)
no contains(KEY_SESSION_TOKEN)
no getBoolean(...)
no if statement checking session presence before showing the screen
The only session-related behavior is logout:

button click calls clearSession() in Profile.java (line 35)
clearSession() removes sessionToken in Profile.java (line 49)
2. Security Implication

The likely implication is that Profile functions as an unguarded screen once launched, because this class does not appear to verify whether a valid session exists before rendering.

A careful wording would be:
Profile.java does not itself enforce session validation.

That matters because storing a token is not the same as enforcing access control. If no code checks the token before allowing access, the token is effectively just state data, not a protection mechanism.

3. What Still Needs Dynamic Testing

Dynamic testing would confirm whether access is actually possible without a valid session in runtime, for example:

Launch Profile directly and see whether it opens without prior login.
Delete SessionPrefs/sessionToken and then navigate or deep-link into Profile.
Log out with clearSession() and check whether the user remains on Profile or can reopen it without re-authentication.
Verify whether any other component outside this class blocks access before Profile starts.
So the static conclusion is strong for this class, but dynamic testing is still needed to confirm app-wide behavior, especially whether some other activity, intent flow, or runtime condition prevents unauthorized access.

